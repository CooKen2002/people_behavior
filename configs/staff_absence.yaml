image_path: "data/raw/work_station_frame"
short_video_path: "data/raw/1809_0s_5s.mp4"
video_path: "data/raw/1809.mp4"
rois_json_path: "data/annotations/work_region.json"

calibration_secs: 10

track_max_disappeared: 30
max_distance: 100
missing_threshold: 90
assign_threshold: 30
away_threshold: 30

obj_model_path: "models/yolov8n.onnx"
pose_model_path: "models/yolov8n-pose.onnx"

kpt_conf_threshold: 0.4

target_indices: [5, 6, 11, 12, 13, 14]

pose_skeleton: [
    [14, 12], # Đầu gối phải -> Hông phải
    [12, 13], # Hông phải -> Hông trái
    [6, 12], # Vai phải -> Hông phải
    [5, 11], # Vai trái -> Hông trái
    [5, 6], # Vai trái -> Vai phải
  ]

iou_threshold_cluster: 0.4
frequency_threshold: 0.2
frequency_threshold: 0.15